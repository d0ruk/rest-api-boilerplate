{
  "watch": ["src", "config"],
  "ext": "ts,json,yml",
  "ignore": ["test", ".git", "node_modules/**/node_modules"],
  "exec": "ts-node -r tsconfig-paths/register --transpile-only src",
  "env": {
    "NODE_ENV": "development"
  }
}
